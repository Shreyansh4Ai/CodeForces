#include <iostream>
using namespace std;

int main() {
    int t;
    cin >> t; // Number of test cases
    while (t--) {
        int n;
        cin >> n;
        cout << (n - 1) << endl; // Number of valid (a, b) pairs
    }
    return 0;
}
